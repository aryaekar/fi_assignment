# Example .env file for Inventory Backend
# Replace values with your actual credentials and secrets

DATABASE_URL=postgresql://<username>:<password>@<host>:<port>/<database>?sslmode=require
JWT_SECRET=your_jwt_secret
PORT=8080 